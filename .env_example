# ------------------------------
# BoardRAG – API Keys Only
# ------------------------------
# Copy this file to `.env` and add your API keys.
# All other configuration is handled in config.py (which you can modify directly).

# ---------------------------------------------------------------------------
# LLM Provider Selection (Optional - defaults to "anthropic")
# ---------------------------------------------------------------------------
# Uncomment to override the provider set in config.py
# LLM_PROVIDER=anthropic

# ---------------------------------------------------------------------------
# API Keys (Required based on provider)
# ---------------------------------------------------------------------------

# For OpenAI models (required for embeddings, even when using Anthropic)
OPENAI_API_KEY=your_openai_api_key_here

# For Anthropic models (required when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ---------------------------------------------------------------------------
# Optional Overrides
# ---------------------------------------------------------------------------
# You can override any config.py setting here if needed:
# GENERATOR_MODEL=claude-3-5-sonnet-20241022
# EMBEDDER_MODEL=text-embedding-3-small
# CHUNK_SIZE=3200
# CHUNK_OVERLAP=400
# CHROMA_PATH=chroma
# DATA_PATH=data 